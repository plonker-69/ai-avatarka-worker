# Minimal additional requirements for AI-Avatarka
# Base image already has torch, torchvision, ComfyUI, etc.

# RunPod serverless
runpod~=1.7.9

# Only add what's missing from base image
aiofiles>=23.0.0

# For Google Drive downloads (if not in base)
gdown>=5.0.0